# Daemonization — macOS packaging, IPC, and keystore hardening for `keriguard_user`

This document covers the daemonization work — turning sentinel/guardian into independent, launchd-supervised macOS background daemons — plus the cleanup items deferred out of the packaging pipeline.

> **Phase numbering note:** This document was renumbered to be internally consistent. Phases are ordered by execution sequence, and every cross-reference below points at the phase number as it appears in *this* document.

---

## Prerequisite state — do NOT re-verify

The following are already complete and are assumed as the starting point for this plan:

- **End-to-end provisioning verification** against the docker-hosted platform: complete (the provisioning *mechanism* is proven end-to-end). **Note:** this was verified with the old `witness=False` default. Per the Witness decision below, the dev environment is **wiped and re-provisioned from scratch with `witness=True`** — this is a fresh first-time provisioning, **not** a migration of an existing AID.
- **`feat-keriguard-build` packaging pipeline** (`Locksmith.spec`/PyInstaller; `Locksmith.app` launches, opens a vault, runs Setup end-to-end): recently built and verified from source.

**Gate:** Do **not** re-verify the packaging build until the daemonization work below — Phase 4 in particular, which adds new PyInstaller targets and entitlements to the same spec/sign pipeline — is substantially in place. Re-verifying now would just re-verify the same build again before it changes. **Current phase: 4.5** — real CI builds cut from fork branches (`arilieb/locksmith`, `arilieb/keriguard-plugin` on `feat-daemon-support`) to prove the Phase 4 pipeline out on GitHub's macOS runners before Phase 1's fork+review-then-merge-then-tag sequence proceeds. This is *not* the Phase 5 manual verification pass — see Phase 4.5 below.

## Dependency graph (critical path)

    Decisions (all settled: headDirPath=--base plugins/keriguard-user; witness=YES; sentinel=SaaS/--config)
            │  ← all three decisions RESOLVED; proceed straight to Phase 1
            ▼
    Phase 1 (upstream patches — CODE COMPLETE on `daemonize-phase1`; review + tag remain)
            │
            ├───────────────────────┐
            ▼                        ▼
    Phase 3 (plugin daemon code)   Phase 2 (pkg cleanup) ─┐
            │                                             │ ← Phase 2 & Phase 4
            │                        Phase 4 (PyInstaller │   done in ONE pass
            │                        + sign + pin) ───────┘   (shared files)
            └───────────┬────────────────────┘
                        ▼
    Phase 4.5 (fork-cut release CI: prove Phase 4's pipeline green on real macOS runners) ← CURRENT
                        │  ← green run here greenlights Phase 1's fork+review → merge → tag
                        ▼
    Phase 5 (verify: cleanup → lifecycle → e2e)

**Sequencing rules baked into the graph:**

1. **The reconciliation decisions are now all resolved** — `headDirPath` stays at its default and Haberies are distinguished via the pre-existing `--base plugins/keriguard-user` flag (no upstream patch), witness = YES, and SaaS mode are decided in the Decisions section below. No open blocker remains before Phase 1.
2. **Phase 1 must land + tag before Phase 4** — pinning needs tags; packaging needs the socket/passcode/heartbeat patches (`--data-dir` was reverted, see Decisions). **The code for 1a, 1b, 1d is complete** (branch `daemonize-phase1`: `sentinel` `3b67423`, `keriguard` `56ee181`, both since amended to drop `--data-dir`); what remains on the critical path is the fork+review pass, merge to `main`, and tagging. **Phase 4.5 inserts a real-CI proof point before that fork+review pass is greenlit** — see below.
3. **Phase 2 and Phase 4 are one pass, not two phases run back-to-back** — they touch the same three files (`Locksmith.spec`, `sign.sh`, `release.ci.yml`); doing them in a single pass avoids re-touching entitlements/signing twice. Phase 3 also feeds Phase 4, but as an ordinary dependency (its outputs are consumed), *not* as a shared-file pairing.
4. **The three `/tmp` socket sites move in lockstep** within a single Phase 1 PR.
5. **Do not re-verify the packaging build** until Phase 4 is substantially in place (see gate above).
6. **Phase 4.5 runs entirely against fork branches, never `main`** — it cuts real `release.ci.yml` releases from `arilieb`'s forks of `locksmith`/`keriguard-plugin` (and consumes `sentinel`/`keriguard` by branch, not tag) purely to validate the Phase 4 pipeline end-to-end on real macOS CI runners before committing to Phase 1's merge+tag sequence. It does not merge or tag anything itself, and a green run there is what unblocks Risk #6.

---

## Decisions

### Two separate daemons (recorded, not yet implemented)

**Two separate long-running processes**, running (macOS-adapted) `kg guardian start` and `sentinel start` independently, mirroring the proven Linux split:

- `keriguard/debian/keriguard-guardian.service` — `ExecStart=.../kg guardian start -c ${KERIGUARD_CONFIG}`, `Restart=on-failure`, `RestartSec=10`.
- `keriguard/debian/keriguard-sentinel.service` — `ExecStart=.../sentinel start -c ${SENTINEL_CONFIG}`, same restart policy.

This reuses both CLIs' tested code paths unchanged, keeps failure domains independent (a WireGuard-apply bug in guardian doesn't take down KEL-watching), and the two already coordinate only through on-disk exported CESR files (`sentinel/src/sentinel/framework/watching.py:76-127`) and a Unix socket (Phase 3b) — a boundary that fully decouples them. No code has been written for either daemon's macOS packaging yet; that's Phase 3/Phase 4.

### `headDirPath` gap (Risk #5) — REVERSED: no `--data-dir` flag needed; use `--base plugins/keriguard-user` instead

**Superseded.** The `--data-dir` decision below was implemented, then reverted once it became clear it was solving a self-inflicted problem. Keeping the history for context:

The original reasoning (now known wrong): `_open_or_create_hab()` tried to put the absolute App-Support directory (`~/Library/Application Support/KERIGuard/keystores`) into `base`, hit `hio.hioing.FilerError` (`Filer.__init__` rejects an absolute `base`), and concluded from that failure that `headDirPath` needed a new CLI override instead. That conclusion doesn't follow: `Filer.__init__` only rejects an *absolute* `base` — a relative, multi-segment `base` (e.g. `"plugins/keriguard-user"`) is completely legal, and is exactly what `locksmith.ui.vaults.drawer`'s base-navigation is built to display as a distinct, navigable folder separate from the root vault list.

**Decision (current): leave `headDirPath` at its default (unset) on both CLIs, and provision/launch the guardian and sentinel Haberies with `base=keystore.SERVER_BASE` (`"plugins/keriguard-user"`)** — the pre-existing `--base`/`-b` flag both `sentinel start` and `kg guardian start`/`up.py` already had before any of this work started. This lands the daemon Haberies at `<default_head>/keri/ks/plugins/keriguard-user/{name,name-sentinel}`, the **same default `headDirPath`** the human vault's own Haberies already use (`locksmith/src/locksmith/ui/vaults/create.py`'s `CreateVaultDialog` never overrides `headDirPath` either), just under a `base` segment that keeps daemon keystores out of the root vault list in `drawer.py`. Two upsides over the reverted `--data-dir` approach:
- Zero new flags on either upstream CLI — both repos build against unmodified `main` for this piece (the `--socket-dir`, `--passcode-file`, and heartbeat patches below are unrelated and still needed).
- Reuses a `headDirPath` location **already proven to work under the sandboxed packaged build** (per the prerequisite state at the top of this document), instead of the App-Support tree, whose entitlements were never actually exercised under a real sandboxed build (see the now-moot Phase 2b/4 `.../keystores/` entry — no longer needed).

Guardian and sentinel still each get their **own Habery / own LMDB environment**, distinguished by `name`/`alias` exactly as before (`--name`/`--alias`, unchanged, still required explicitly on both daemons' launchd `ProgramArguments` per Phase 1c/3c) — `base` here only differentiates the *pair* from the human vault, not the two daemons from each other.

Consequences baked into the rest of this plan:
- `provisioning.py`'s `_open_or_create_hab()` passes `base=keystore.SERVER_BASE`, no `headDirPath` override.
- Both daemons' launchd `ProgramArguments` (Phase 3c) pass `--base plugins/keriguard-user` **plus explicit `--name`/`--alias`** matching provisioning's values, instead of `--data-dir`.
- `keystore.py` no longer needs a `keystores_dir()` App-Support path for the Haberies themselves; `SERVER_BASE = "plugins/keriguard-user"` replaces it. The rest of the App-Support tree (bran, sentinel config/socket, heartbeat, logs) is unaffected.

### Witness for the guardian AID — DECIDED: **YES, the guardian AID gets a witness**

Witness provisioning for a server AID is a separate OTP-based flow (`reserve_witness_for_server` → OOBI-load → `authenticate_witness` → `rotate_witness`), **not** a `number_of_witnesses` field on the registration `doc`. The current code defaults `witness=False`, `toad=0`; **this default is being overridden — the guardian AID will be provisioned with a witness.**

Consequences baked into the rest of this plan:
- Provisioning must run the full OTP-based flow via `provisioning.py`'s `bootstrap_server_identity` (`reserve_witness_for_server` → OOBI-load → `authenticate_witness` → `rotate_witness`) with `witness=True` and a non-zero `toad`, **before** Phase 4 packages the daemon. In this dev environment this is a **from-scratch provisioning after wiping the environment** — there is no existing `witness=False` AID to migrate.
- The guardian launchd `ProgramArguments` (Phase 3c) reflect the witnessed shape (`witness=True` two-Habery bootstrap).
- **Correction (verified against current `connecting.py`, `sentinel` `3b67423`): the OTP-secret path is *not* configurable and is *not* under App-Support.** `authenticate_witness()` (`sentinel/src/sentinel/framework/connecting.py:213-235`) hardcodes the write path to `~/.keriguard/{server_hab.pre}` (mode `0600`, keyed by the guardian AID, created once during the OTP handshake inside `connect_to_healthkeri`/`bootstrap_server_identity`). It is **write-only** — grepped both the `sentinel` and `keriguard` repos and nothing reads it back after the initial rotation; it is not consumed at guardian-daemon start time, so there is no "ensure it exists before bootstrapping" step for `guardian_launch.py` to perform. Treat `~/.keriguard/` (not `.../KERIGuard/server.otp`) as the real path in the entitlements audit (Phase 2b / Phase 4) and the Phase 3d file inventory — it needs the same kind of `temporary-exception.files.absolute-path.read-write` coverage as `~/.locksmith/` already gets, since it also sits outside both the sandbox container and `~/Library/Application Support/KERIGuard/`.

### Sentinel mode — DECIDED: **SaaS mode (`--config <yaml>`)**

`sentinel start` **hard-requires `--config <yaml>` in SaaS mode.** Confirmed at `sentinel/src/sentinel/app/cli/commands/start.py`: `merge_config_and_args()` (line 107) raises `ValueError("SaaS mode (local: false) requires --config")` at line 134 when `--local` is unset, and `server_name`/`server_alias` can't be passed as flags in that mode at all.

**Decision: the sentinel daemon runs in SaaS mode** (plugin `credential_source = "healthKERI"`). This means the launchd plist **must not** use flag-based invocation; it points `--config` at a generated YAML file.

Consequences baked into the rest of this plan:
- `sentinel_launch.py` (Phase 3a) must **render a YAML config file from the plugin's settings and write it to disk before writing the plist.** Store the generated YAML under the daemon's App-Support tree (e.g. `~/Library/Application Support/KERIGuard/sentinel/config.yaml`) so it lives with the other sentinel daemon state and is covered by the same entitlements.
- The sentinel plist's `ProgramArguments` are `sentinel start --config ~/Library/Application Support/KERIGuard/sentinel/config.yaml --base plugins/keriguard-user --uxd --export-dir <export_dir>` (plus `--passcode-file` per Phase 1b, and explicit `--name`/`--alias` per the Phase 1c correction below — SaaS mode rejects `--local`, but `--name`/`--alias` are still required since they can't be expressed in the YAML).
- Add the generated `config.yaml` path to the entitlements audit (Phase 2b / Phase 4) and the socket/file inventory (Phase 3d).

---

## Phase 1 — Upstream repo patches (`sentinel` + `keriguard`) [CODE COMPLETE — review + tag pending]

Group these — they touch the same upstream files, and Phase 4 depends on all of them landing **and being tagged**. These are patches to the `keriguard`/`sentinel` repos, **not** `keriguard-plugin`.

> **Status:** 1a, 1b, and 1d are implemented and unit-tested on branch `daemonize-phase1` in both `sentinel` (commit `3b67423`) and `keriguard` (commit `56ee181`), both still on top of `main` in each repo. **1c (`--data-dir`) was implemented and then reverted** — see the Decisions section correction above; both upstream repos are back to building clean against unmodified `main` for that piece. Full test suites pass in both repos after the revert (`sentinel`: 227 passed; `keriguard`: 174 passed), including tests for the `socket_dir`/`--passcode-file`/heartbeat behavior (the `head_dir_path`/`data_dir`-specific tests were removed along with the flag). **Deliberately not tagged yet** — per the user's request, these branches should go through a fork+review pass first so `main` in either upstream repo isn't tagged before the daemonization approach is confirmed end-to-end. Tagging (needed for Phase 4 pinning) is still an open step once that review lands. Two follow-ups noticed but out of scope for Phase 1: (1) `sentinel`'s venv was missing `pytest-asyncio` despite being a declared dependency — installing it fixed 14 unrelated pre-existing test failures, a pure local-env fix, no code change; (2) `keriguard`'s venv was missing `pyotp` (a `sentinel` dependency) — also a local-env-only fix needed to import `guardian/up.py` for manual CLI verification.
>
> **Implementation detail that ripples into later phases:** the socket relocation is exposed as a **`--socket-dir` flag on `sentinel start`** (default `/tmp`), so the macOS override is a plist argument, not a compiled-in constant — Phase 3a/3c must emit it, and it belongs in the Phase 3d inventory.

### 1a. Sentinel watcher-add socket — relocate off `/tmp` (Risk #1)  [COMPLETE]

`sentinel watcher add` talks to a running `sentinel start --uxd` process via `LocalWatcherConnector` over a Unix socket (`sentinel/src/sentinel/framework/watching.py:227-308`, confirmed). Both client and server hardcode `f"/tmp/sentinel_{hab.pre}.sock"`.

Thread a `socket_dir` parameter through **all three lockstep sites**, defaulting to `/tmp` for existing Linux/Debian packaging, overridden to `~/Library/Application Support/KERIGuard/sentinel/` for the macOS launchd agent (matching the existing `~/Library/Application Support/KERIGuard/helper.sock` convention):
- client — `watching.py:250`
- server (`setup_local`) — `sentineling.py:106` (file confirmed at `sentinel/app/sentineling.py`, not `framework/`)
- server (`setup_hk`) — `sentineling.py:186`

> **Note (SaaS mode):** since the daemon runs SaaS mode, the active server-side socket setup is `setup_hk` (`sentineling.py:186`), not `setup_local`. Both sites still move in lockstep, but `setup_hk` is the one exercised at runtime by this daemon — verify the relocated path via the `setup_hk` path in Phase 5.

Coordinate the parameter threading and the macOS override value together in **one PR** against `sentinel`.

**Implemented (`sentinel` `3b67423`):** a `socket_dir` parameter is threaded through `LocalWatcherConnector`, `watcher/add.py`, `setup_local`, and `setup_hk`, and surfaced as a **`--socket-dir` flag on `sentinel start`** defaulting to `/tmp` — existing Linux/Debian behavior is byte-for-byte unchanged when the flag is omitted. The macOS value (`~/Library/Application Support/KERIGuard/sentinel/`) is supplied by the launchd plist (Phase 3c), and the plugin's direct `LocalWatcherConnector` use (Phase 3a) must pass the same `socket_dir` or it will look for the socket in `/tmp`.

**Bug found + fixed (via Phase 4.5 manual runtime verification):** the socket filename itself (`sentinel_{hab.pre}.sock`, full AID, ~44 chars) was never adjusted for the new macOS `socket_dir`. Nested three levels under `~/Library/Application Support/KERIGuard/sentinel/`, the resulting path (e.g. `.../sentinel_EBE_R5VZLYNGiebVKr9E7LiIGGM2J4deqV7A5Zzi1DT8.sock`, 120 bytes) exceeds macOS's 104-byte `sockaddr_un.sun_path` limit, so every connect failed with `AF_UNIX path too long` — this was unconditional, not intermittent, once the socket moved off `/tmp`. The full AID was only load-bearing for uniqueness under the old shared-`/tmp` design (multiple users/instances on one Linux box); under the per-user, per-install macOS `socket_dir`, only one sentinel ever writes into that directory, so the AID doesn't need to be full-length there. Fixed by adding `sentinel_socket_filename()` (`sentinel/src/sentinel/framework/watching.py`), shared by all three lockstep sites (`watching.py`'s `LocalWatcherConnector`, `sentineling.py`'s `setup_local`/`setup_hk`), which truncates to the AID's last 8 characters (`sentinel_{aid[-8:]}.sock`, e.g. `sentinel_5Zzi1DT8.sock`) — the resulting macOS path is 84 bytes, 20 bytes under the limit. Applies identically on Linux/Debian (still well under its 108-byte limit there); all 227 `sentinel` tests updated and passing.

### 1b. Add `--passcode-file` to both CLIs  [COMPLETE]

`core/keystore.py`'s `load_or_create_bran()` already stores the bran at `~/Library/Application Support/KERIGuard/server.bran` (mode `0600`, `stat.S_IRUSR|S_IWUSR`, owner-only), independent of the human vault's own passcode. **The bran file *is* the passcode file** — the daemon's passcode is exactly the contents of `server.bran`, so `--passcode-file` points at that same file (there is no second, separate passcode secret). **But** neither daemon CLI has a `--passcode-file` option today (guardian `start.py`: only `--base`/`-b` at line 75 and `--passcode` at lines 78-82; sentinel `start.py`: only `--passcode`/`-p` at lines 33-38) — a plain string visible via `ps`.

Add a `--passcode-file` flag to both:
- `keriguard/src/keriguard/app/cli/commands/guardian/start.py`
- `sentinel/src/sentinel/app/cli/commands/start.py`

**Read semantics:** the `--passcode-file` reader must `.read().strip()` the file (strip trailing newline/whitespace) and decode as UTF-8, symmetric with how `load_or_create_bran()` writes `server.bran`. A stray trailing `\n` or an encoding mismatch produces a "wrong passcode" failure that looks unrelated to the file — pin the write/read convention on both sides so it can't drift.

**Implemented (`sentinel` `3b67423`, `keriguard` `56ee181`):** `--passcode-file` added to `sentinel start` and `kg guardian start`; the reader reads and strips the file as specified. **Precedence: `--passcode` wins if both are given** — so a plist that passes `--passcode-file` must not also pass `--passcode`, or the file is silently ignored.

Flag Keychain storage as a v2 hardening step once the upstream CLIs support pluggable passcode sources.

### 1c. ~~Add a `--data-dir` (`headDirPath`) flag to both CLIs~~ — DROPPED, no CLI patch needed

Originally implemented as an absolute `--data-dir`/`headDirPath` override on both CLIs (`sentinel` `3b67423`, `keriguard` `56ee181`), then **reverted** — see the Decisions section correction above. The premise (a Habery provisioned under an absolute App-Support path needs a `headDirPath`-override flag to be reopened) was itself the mistake: `headDirPath` never needed to move off its default in the first place. `provisioning.py` and the launchd plists now use the pre-existing `--base plugins/keriguard-user` flag instead, at the default `headDirPath`.

**`--name`/`--alias` contract (unchanged by the revert):** `provisioning.py`'s `_open_or_create_hab()` passes `base=keystore.SERVER_BASE` (the same value) for **both** Haberies. The two Haberies are still distinguished from *each other* by `name`/`alias`: guardian uses `name`/`alias` as provisioned (the vault's own name/alias), sentinel uses `f"{name}-sentinel"`/`f"{alias}-sentinel"`. Both `sentinel start` and `kg guardian start`/`up.py` **already expose `--name`/`-n` and `--alias` flags today** — no CLI patch needed for this piece either. Each daemon's launchd `ProgramArguments` (Phase 3c) must pass **explicit `--name`/`--alias`** matching what `provisioning.py` used for that daemon's Habery — do **not** rely on either CLI's default `--name`. A mismatch silently opens an empty/wrong Habery rather than the provisioned one (caught by the Phase 5 "reopen via `--base`, no fallback" check).

### 1d. Guardian heartbeat touch  [COMPLETE]

Guardian has no local control/status socket on any platform today (Linux uses systemd D-Bus, which has no macOS analog). Adding a new status socket would mean modifying `sentinel.framework.runner.run()` (`sentinel/src/sentinel/framework/runner.py:19`→EOF — a synchronous entry point that optionally builds a `Habery`/`AppBaser`, then `asyncio.run(_async_run(...))`, which creates a `FileWatchingService`, starts it as a task, and blocks until `SIGINT`/`SIGTERM` or task completion) to run a second listener — new, unproven code. Instead, use a heartbeat file.

`keriguard/src/keriguard/app/sentinel/handler.py`'s `KeriguardEventHandler` has **no** single per-poll-cycle "success" callback — it exposes three separate handlers, `on_kel` (line 45), `on_tel` (line 68), `on_credential` (line 89).

**Preferred**: add the heartbeat touch inside `runner.py`'s `_async_run`/`FileWatchingService` poll loop, at the point it confirms a full cycle completed without exception (reflects real service health, not "some event fired"). Read `FileWatchingService`'s poll-loop structure first to pick the exact point. Fallback: touch at the end of all three handler methods.

Heartbeat path: `~/Library/Application Support/KERIGuard/guardian.heartbeat`.

**Implemented (`sentinel` `3b67423`) — the preferred option, not the handler fallback:** a `heartbeat_path` parameter was added to `FileWatchingService`, `runner.run()`, and `_async_run()`, and the file is touched after each poll cycle completes without raising. `handler.py`'s three per-event handlers are untouched. Consequence for Phase 3: staleness thresholds in `guardian_check.py` must be derived from the poll interval (a healthy but idle daemon still touches the file every cycle), and the heartbeat is written by the **sentinel-framework runner the guardian embeds**, so the path must be passed in from the guardian invocation rather than assumed.

---

## Phase 2 — Packaging pipeline cleanup [2a/2b/2c CODE COMPLETE — tagging + real sandboxed smoke test pending]

These three items were scoped out of the packaging pipeline so build-verify/merge-to-`locksmith`-main wouldn't block on them. **Do them together with Phase 4**, since Phase 4 touches the same files (`Locksmith.spec`, `sign.sh`, `release.ci.yml`) again to add the two new daemon executables — one pass avoids touching entitlements/signing twice.

> **Status:** All three sub-items (2a–2c) are implemented.
> - `keriguard-plugin` (this repo, commit `e59027a` on `feat-daemonization`): `plugins/user/pyproject.toml` and `plugins/admin/pyproject.toml` bumped `0.0.1` → `0.1.0`; added `.github/workflows/ci.yml` (installs both plugins into a clean venv, import-smoke-tests `keriguard_user`/`keriguard_admin`). **Note:** `keriguard-admin` depends on `locksmith==0.0.1`, which is *not* the PyPI package of that name (PyPI has an unrelated `locksmith` at versions 0.1–1.1) — the CI workflow installs the real `locksmith` from `git+https://github.com/healthKERI/locksmith.git` before installing the admin plugin so the pin resolves correctly instead of failing or grabbing the wrong package. `release.ci.yml`'s own plugin-flavor job has the same latent issue but is out of scope for this pass (it already runs `pip install -e .` inside the `locksmith` checkout, which happens to satisfy it there).
> - `locksmith` (uncommitted local edits, not yet committed/pushed — pending your review): `.github/workflows/release.ci.yml`'s plugin-flavor job now pins both plugin installs to `@v0.1.0` instead of the floating default-branch URL; `scripts/sign.sh`'s heredoc that regenerated an independent `entitlements.plist` copy is removed, so the existing `--entitlements entitlements.plist` codesign call now signs against the single checked-in file; `entitlements.plist` gained `com.apple.security.files.user-selected.read-write` plus a `com.apple.security.temporary-exception.files.absolute-path.read-write` array covering `~/Library/Application Support/KERIGuard/` and `~/.locksmith/` (folding in the union of what the old heredoc granted plus the new paths from the Decisions section); `scripts/fetch_keriguard_helper.py` now requires `KERIGUARD_HELPER_VERSION` to be set and exits 1 with an error instead of defaulting to `"current"`.
>
> **Still open:**
> 1. **Tag `v0.1.0` doesn't exist yet** — `release.ci.yml`'s pin references it but nothing has been tagged/pushed (deliberately deferred, matching the Phase 1 fork+review-before-tagging pattern). Cut the tag once this pass is reviewed.
> 2. **The `locksmith` edits above are local/uncommitted** — not yet committed or pushed to `arilieb/locksmith`, pending review.
> 3. **Final sign-off is still deferred to Phase 5** (per the existing note below) — none of 2b's entitlements changes have been exercised under a real sandboxed build yet.

### 2a. Pin the plugin dependency (Risk #2)

`locksmith/.github/workflows/release.ci.yml`'s plugin-flavor build job installs `keriguard-user`/`keriguard-admin` via two unpinned `pip install git+https://github.com/healthKERI/keriguard-plugin.git#subdirectory=plugins/{user,admin}` calls (`release.ci.yml:209-210` — no explicit `@main`, but a bare URL floats to the default branch, same unpinned effect). `keriguard-plugin` still has zero git tags and zero CI.
- Bump `plugins/user/pyproject.toml` and `plugins/admin/pyproject.toml`'s static `version = "0.0.1"` and start tagging releases (semver).
- Pin `release.ci.yml`'s install step to a tag instead of `@main`.
- Add a minimal CI workflow to `keriguard-plugin` (no `.github/workflows/` directory exists yet) — at minimum, install both plugin packages into a venv and run an import smoke test (`import keriguard_user`, `import keriguard_admin`) on push/PR.

### 2b. Fix the entitlements duplication + gap

`locksmith/scripts/sign.sh` does not read the checked-in `locksmith/entitlements.plist` — it regenerates an independent copy via a heredoc (`sign.sh:101-126`) and signs against that copy, so the two can silently drift.
- Change `sign.sh` to pass the checked-in `entitlements.plist` path directly to `codesign --entitlements` (remove the heredoc).
- `entitlements.plist` declares `app-sandbox=true` plus `network.client`/`network.server` but no `com.apple.security.files.*` entitlement, despite `KERIGuardUserBaser`'s LMDB store, the user-chosen WireGuard config directory, the `~/Library/Application Support/KERIGuard/` keystore/bran files, and `~/.locksmith/*.pem` all doing file I/O outside the sandbox container. Add the needed entitlement (`com.apple.security.files.user-selected.read-write` at minimum; the App-Support and `~/.locksmith` paths are outside the container and outside user-selection, so they likely also need a `com.apple.security.temporary-exception.files.absolute-path.read-write` array entry, or relocating those paths under the app's sandbox container).
- **Also cover the paths introduced by the Decisions section:** the guardian OTP-secret artifact (`~/.keriguard/{server_hab.pre}` — hardcoded outside App-Support, see Decisions correction above), and the generated sentinel SaaS config (`~/Library/Application Support/KERIGuard/sentinel/config.yaml`). The guardian/sentinel keystore trees themselves no longer need a separate App-Support entitlement — they now live at the default `headDirPath` under `--base plugins/keriguard-user`, the same location the human vault's own Haberies already use (see Decisions correction).
- **Final sign-off is deferred to Phase 5** (requires a real sandboxed smoke test — this has never been exercised under a real sandboxed build).

### 2c. Fix the `KERIGUARD_HELPER_VERSION` default (Risk #3)

`locksmith/scripts/fetch_keriguard_helper.py:101` defaults to the literal string `"current"` if the env var is unset — an implicit-latest footgun for a security-sensitive embedded binary that owns the WireGuard tunnel. Make it a required, non-defaulted CI input (raise/exit if unset rather than defaulting).

---

## Phase 3 — New plugin daemon code (`keriguard-plugin`) [CODE COMPLETE — untestable until Phase 4 embeds the frozen binaries]

> **Status:** All of 3a–3d implemented on `feat-daemonization`.
> - `setup/page.py`'s `_provision_server_identity` now passes `witness=True` to `bootstrap_server_identity` — this was a gap found while starting Phase 3: the Decisions section already settled "guardian AID is witnessed", but the actual provisioning call site was still requesting the old `witness=False` default. Without this fix the daemons' witnessed `ProgramArguments` shape (Phase 3c) would have been bootstrapped against an unwitnessed AID.
> - **Correction found and applied to the Decisions section while gathering Phase 3 context:** the guardian's OTP-secret artifact does **not** land at `~/Library/Application Support/KERIGuard/server.otp` as originally documented. `authenticate_witness()` (`sentinel/src/sentinel/framework/connecting.py:213-235`, already on `daemonize-phase1`) hardcodes the path to `~/.keriguard/{server_hab.pre}`, write-only, never read back at daemon start. Decided (user confirmed): accept the real path and document it, rather than patch upstream `sentinel` again on an already-review-pending branch. See the corrected Decisions/Phase-2b/Phase-4/inventory/risk-table entries above.
> - New: `core/daemon_launch.py` (shared plist-template rendering + `launchctl bootstrap` helper, whole-token `{PLACEHOLDER}` substitution — never partial string formatting, so a path containing `{`/`}` can't be misparsed), `core/sentinel_launch.py`, `core/guardian_launch.py`, `core/sentinel_check.py`, `core/guardian_check.py`, `core/daemon_watch.py` (the `LocalWatcherConnector` direct-call wrapper from the 3a bullet below — there was no pre-existing `sentinel watcher add` shell-out in this repo to replace; this is new functionality mirroring the tail of `kg guardian up`), plus `resources/launchd/com.healthkeri.keriguard.{guardian,sentinel}.plist` templates.
> - Plist templates were verified end-to-end with a standalone script: rendered against dummy values and parsed back with `plistlib` to confirm well-formed XML and correct `ProgramArguments`. **Pitfall hit and fixed:** an XML comment containing a literal `--` (e.g. explanatory prose) makes the plist not well-formed per the XML spec — the template comments were rewritten to avoid double-hyphens; keep this in mind for any future edits to these `.plist` files.
> - `_write_saas_config` (in `sentinel_launch.py`) was dry-run against a fake settings object and confirmed to omit `bran`/`passcode` from the generated YAML (the daemon gets its passcode via `--passcode-file`, resolved before `SentinelConfig`'s merge step reads `config.bran` — embedding it in the YAML too would be a needless plaintext-secret duplication).
> - Wired into `plugin.py`: `_on_initialization_done` now also calls a new async `_launch_daemons()` (bootstraps both agents, then registers the issuer-AID watch via `daemon_watch.register_issuer_watch` if the sentinel daemon launch succeeded), and `_on_keriguard_menu_opened` now also logs guardian/sentinel launchd + heartbeat health via a new `_check_daemon_health()`.
> - Full `import keriguard_user` (and submodules) smoke-tested locally against real `PySide6`/`locksmith`/`sentinel`/`keriguard` installs — no import errors.
>
> **Still open / cannot be exercised yet:**
> 1. **No embedded frozen binaries yet** — `find_daemon_executable()` looks for `kg-guardian`/`sentinel-daemon` under the frozen bundle's `Contents/Resources/`; those only start existing once Phase 4 adds the PyInstaller `Analysis`/`EXE()` targets. Until then, `launch_sentinel_daemon`/`launch_guardian_daemon` always no-op (log a warning, return `False`) even on a frozen build.
> 2. **Plist templates aren't yet added as PyInstaller `datas`** — `find_plist_template()` has a frozen-build lookup path (`Contents/Resources/launchd/`) mirroring the executable lookup, but nothing in `Locksmith.spec` populates it yet; add this alongside the Phase 4 executable-embedding work.
> 3. **No live `launchctl bootstrap` / daemon-lifecycle test performed** — correctly deferred to Phase 5 per the packaging-build gate (this doesn't change with Phase 3 code landing; still needs Phase 4's frozen binaries first).
> 4. **`daemon_watch.register_issuer_watch` reopens the guardian Habery by `--passcode-file`-equivalent bran read** (`keystore.load_or_create_bran`-compatible convention, but actually reads `settings`/`keystore.BRAN_PATH` directly) — not yet exercised against a real running sentinel socket; the retry-on-`ConnectionError` loop (5× 3s, mirroring `kg guardian up`'s tail) is untested against real timing.

In `plugins/user/src/keriguard_user/core/`, following the existing `helper_launch.py`/`helper_check.py` template:
- `launch_helper_app()` (`helper_launch.py:17`) runs `subprocess.Popen(["open", "-a", str(app_path)])` (line 40) to launch `KERIGuardHelper.app`.
- `is_helper_installed()` (`helper_check.py:23`) runs `launchctl print gui/{uid}/{HELPER_AGENT_LABEL}` and checks the returncode for a health check.

### 3a. New files

- `sentinel_launch.py` / `guardian_launch.py` — write the filled-in launchd plist and run `launchctl bootstrap gui/$(id -u) <plist>` for each daemon.
  - **`sentinel_launch.py` (SaaS):** first render a YAML config file from the plugin's settings and write it to `~/Library/Application Support/KERIGuard/sentinel/config.yaml`, **then** write the plist whose `ProgramArguments` point `--config` at that file (and pass `--base plugins/keriguard-user` and `--socket-dir`). Do not emit `--name`/`--alias`/`--local` (rejected in SaaS mode).
  - **`guardian_launch.py` (witnessed):** the plist's `ProgramArguments` reflect the `witness=True` two-Habery bootstrap and pass `--base plugins/keriguard-user`. No OTP pre-check needed — the OTP artifact at `~/.keriguard/{server_hab.pre}` (see Decisions correction) is written once during provisioning's `connect_to_healthkeri` handshake and never read back by the daemon at start time.
- `sentinel_check.py` / `guardian_check.py` — health checks, following the `is_helper_installed()` pattern (`launchctl print gui/<uid>/<label>`, plus, for guardian, the heartbeat-file staleness check from Phase 1d — threshold derived from the poll interval, since the touch is per poll cycle, not per event).
- Bundled launchd plist templates under a new `plugins/user/src/keriguard_user/resources/launchd/` (`com.healthkeri.keriguard.sentinel.plist`, `com.healthkeri.keriguard.guardian.plist`) — **relocated during Phase 4** from an initial `plugins/user/resources/` (outside `src/`, so it wasn't packaged at all under a real `pip install`); see the Phase 4 status note.
- Have the plugin call `LocalWatcherConnector` directly (or a thin wrapper) rather than shelling out to `sentinel watcher add` — passing the same `socket_dir` the daemon was started with (Phase 1a), not the `/tmp` default.

### 3b. Plain frozen CLI binaries, not signed `.app` bundles

`KERIGuardHelper.app` needs the full Xcode/System-Extension/notarization pipeline because it owns the WireGuard tunnel via `NEPacketTunnelProvider`. `kg guardian start` never touches the tunnel directly — it delegates tunnel control to KERIGuardHelper's existing Unix-socket IPC. `keriguard/src/keriguard/core/systeming.py`'s `_send_helper_request` (~line 164) already implements this: line-delimited JSON over a Unix socket at `_HELPER_SOCKET_PATH` (`~/Library/Application Support/KERIGuard/helper.sock`, matching `IPCServer.defaultSocketPath()` in the separate `keriguard-helper` Swift project). So freeze the two new daemons with PyInstaller and sign them with the same `codesign` loop `sign.sh` already runs — no new Xcode pipeline, no new notarization pipeline.

### 3c. Supervision — bundled launchd agents, not `SMAppService`

`SMAppService.agent(plistName:)` is Swift/`ServiceManagement`-only and requires a `.app` bundle identity these daemons don't have. Ship plist templates as bundled resources; at first successful provisioning (already complete), `sentinel_launch.py`/`guardian_launch.py` write the filled-in plist (executable path, `--base plugins/keriguard-user`, `--passcode-file` per Phase 1b, plus the SaaS `--config` YAML for sentinel) to `~/Library/LaunchAgents/com.healthkeri.keriguard.{sentinel,guardian}.plist` and run `launchctl bootstrap gui/$(id -u) <path>`. `RunAtLoad=true` plus `KeepAlive` gives crash-restart behavior matching the Debian units' `Restart=on-failure`.

`ProgramArguments` mirror the existing CLI invocations, reflecting the settled decisions:
- **Guardian (witnessed; `--base` + explicit `--name`/`--alias`):** `kg guardian start --base plugins/keriguard-user --name <guardian_name> --alias <guardian_alias> --sentinel-aid <sentinel_hab.pre> --sentinel-export-dir <export_dir> --passcode-file ~/Library/Application Support/KERIGuard/server.bran ...` (two-Habery bootstrap shape — `server_hab`/`sentinel_hab`/`witness=True` — confirmed at `keriguard/src/keriguard/app/cli/commands/guardian/up.py:244-248`, which already matches the witnessed shape).
- **Sentinel (SaaS; `--base` + explicit `--name`/`--alias`):** `sentinel start --config ~/Library/Application Support/KERIGuard/sentinel/config.yaml --base plugins/keriguard-user --socket-dir ~/Library/Application Support/KERIGuard/sentinel --name <sentinel_name> --alias <sentinel_alias> --uxd --export-dir <export_dir> --passcode-file ~/Library/Application Support/KERIGuard/server.bran`. **No** `--local` — SaaS mode lives in the generated YAML (per the Decisions section), but `--name`/`--alias` **are** required here, unlike flag-based sentinel invocations, precisely because they can't be expressed inside that YAML.

> **Note:** `<guardian_name>`/`<guardian_alias>` and `<sentinel_name>`/`<sentinel_alias>` are the values that distinguish the two Haberies from *each other* under the shared `--base plugins/keriguard-user` — `provisioning.py` passes the same `base` for both Haberies and instead names them `name`/`alias` (guardian) and `f"{name}-sentinel"`/`f"{alias}-sentinel"` (sentinel). Both CLIs already support `--name`/`--alias` today; they must be passed explicitly (matching what provisioning wrote) rather than defaulted.
> **Note:** `--passcode-file` points at `server.bran` because the bran file *is* the passcode file (Phase 1b) — both daemons read the same file. This is intentional, not a copy-paste of two distinct secrets. Do **not** also pass `--passcode`: it takes precedence and would silently shadow the file.
> **Note:** `--socket-dir` must be passed explicitly on the sentinel invocation — Phase 1a kept the flag's default at `/tmp` so Linux/Debian behavior is unchanged, which means omitting it on macOS silently reverts to the `/tmp` socket. 
> **Note:** the daemon `kg` we freeze is plain `kg guardian start` (`kg = "keriguard.app.cli.kg:main"`), **not** the unrelated `kg-sentinel = "keriguard.app.sentinel.main:main"` entry point.

### 3d. Resulting socket/file inventory

- `~/Library/Application Support/KERIGuard/helper.sock` — existing, unchanged.
- `<default_head>/keri/ks/plugins/keriguard-user/` — provisioned Habery trees, at the same default `headDirPath` the human vault's own Haberies use, reopened by both daemons via `--base plugins/keriguard-user`; each daemon opens its **own `name`-distinguished Habery** under it (a distinct Habery = distinct LMDB environment, no shared-writer contention — see Decisions and the Phase 1c `--name`/`--alias` contract). No longer inside the App-Support tree, and no longer needs its own entitlements entry.
- `~/Library/Application Support/KERIGuard/sentinel/{sentinel_hab.pre}.sock` — relocated from `/tmp` via `sentinel start --socket-dir` (macOS only, Phase 1a; exercised via `setup_hk` in SaaS mode). Any in-process `LocalWatcherConnector` use must pass the same `socket_dir`.
- `~/Library/Application Support/KERIGuard/sentinel/config.yaml` — generated SaaS config, written by `sentinel_launch.py` before the plist.
- `~/Library/Application Support/KERIGuard/server.bran` — bran = daemon passcode file (read via `--passcode-file`), mode `0600` (Phase 1b).
- `~/.keriguard/{server_hab.pre}` — guardian witness OTP-secret artifact, mode `0600`, hardcoded path outside App-Support (see Decisions correction), write-only/not read at daemon start.
- `~/Library/Application Support/KERIGuard/guardian.heartbeat` — guardian liveness (Phase 1d).
- No new control/status socket for guardian (Phase 1d — heartbeat file instead).

---

## Phase 4 — CI/CD & PyInstaller integration [mostly implemented — untestable until Phase 1 tags land]

Depends on Phase 1 patches being **tagged** (for pinning) and landed (for the socket/passcode/heartbeat behavior). Do this pass together with Phase 2 (shared files).

> **Status:** Implemented in `locksmith` (uncommitted local edits, same pending-review state as Phase 2) and `keriguard-plugin` (`feat-daemonization`, uncommitted).
> - **Packaging gap found and fixed first:** `plugins/user/resources/launchd/*.plist` (added in Phase 3) lived *outside* `src/`, at `plugins/user/resources/`. `pyproject.toml`'s `[tool.setuptools.packages.find]` only packages `src/` — a `pip install` of `keriguard-user` (the exact path `release.ci.yml`'s `build-macos-keriguard` job uses) would silently ship with **no plist templates at all**, and `daemon_launch.py`'s dev-fallback path (`Path(__file__).resolve().parents[3]`) would also break under a non-editable install (site-packages doesn't preserve the `plugins/user/` repo layout). Fixed by moving the resources to `plugins/user/src/keriguard_user/resources/launchd/` (inside the package), adding `[tool.setuptools.package-data]` (`keriguard_user = ["resources/launchd/*.plist"]`) to `plugins/user/pyproject.toml`, and updating `daemon_launch.py`'s `_RESOURCES_LAUNCHD_DIR` to `parents[1]` (was `parents[3]`). Verified locally: `find_plist_template()` resolves both templates against the moved path in the editable-install dev venv.
> - `keriguard`'s `sentinel/pyproject.toml`'s `sentinel = "sentinel.app.cli.sentinel:main"` and `keriguard/pyproject.toml`'s `kg = "keriguard.app.cli.kg:main"` are `console_scripts` (module:function), not script files, so PyInstaller's `Analysis` needs a real entry file — added thin wrapper scripts `locksmith/scripts/entrypoints/{kg_guardian_entry,sentinel_daemon_entry}.py` that just call the console-script's `main()`.
> - `Locksmith.spec`: added two guarded (`is_plugin_installed("keriguard_user")`) standalone (non-onedir, not folded into the `Locksmith.app` `COLLECT`/`BUNDLE`) `Analysis`/`PYZ`/`EXE` targets, `kg-guardian` and `sentinel-daemon`, built to `--distpath` (`build/macos/`) alongside `Locksmith.app` rather than inside it — the existing `BUNDLE()`-based `COLLECT` tree becomes `Contents/MacOS/`, and these need to land in `Contents/Resources/` instead, so (mirroring how `KERIGuardHelper.app` is already handled) a **post-build copy script**, not a PyInstaller `datas` destination hack, does the embedding.
> - New `locksmith/scripts/embed_daemons.py` (same no-op-unless-`keriguard_user`-installed guard as `fetch_keriguard_helper.py`): copies `kg-guardian`/`sentinel-daemon` from `--distpath` into `Locksmith.app/Contents/Resources/`, restores the executable bit, then locates the installed `keriguard_user` package via `importlib.util.find_spec` and copies its `resources/launchd/` into `Contents/Resources/launchd/`. Wired into `release.ci.yml`'s `build-macos-keriguard` job, between the PyInstaller build step and the existing `fetch_keriguard_helper.py` step.
> - `sign.sh`: added a signing loop for the two daemon executables (plain hardened-runtime `codesign --options runtime`, no `--entitlements` — they're unsandboxed launchd CLI daemons, not the sandboxed `Locksmith.app` executable), placed before the main-executable/bundle signing steps.
> - `entitlements.plist`: added `~/.keriguard/` to the `temporary-exception.files.absolute-path.read-write` array (the guardian OTP-secret artifact path from the Decisions correction — was missing; `~/Library/Application Support/KERIGuard/` already covers `keystores/`, `sentinel/config.yaml`, `sentinel/{pre}.sock`, `server.bran`, and `guardian.heartbeat` as a directory-prefix entry, no separate entries needed for those).
> - **Fixed (found during a from-scratch manual test):** the two daemon executables were embedded as bare Mach-O binaries directly under `Contents/Resources/{kg-guardian,sentinel-daemon}`, with no `Info.plist`/`CFBundleName`. macOS's Background Items / Login Items UI (System Settings → General → Login Items & Extensions) falls back to the code signature's Common Name when a launchd-registered executable has no bundle identity — both daemons showed up under the developer's own Developer ID Application name instead of a KERIGuard-branded name. Fixed by wrapping each executable in its own minimal `.app` bundle (`KERIGuardGuardian.app`/`KERIGuardSentinel.app`, `Contents/MacOS/<exe>` + a generated `Contents/Info.plist` with `CFBundleName`/`CFBundleDisplayName`/`LSUIElement`/`LSBackgroundOnly`) — see `keriguard_user.core.daemon_app_info.DAEMON_APP_INFO` (single source of truth for the app names/bundle ids), `locksmith/scripts/embed_daemons.py` (builds the bundles), `locksmith/scripts/sign.sh` (signs executable then bundle, inner-to-outer), and `daemon_launch.find_daemon_executable()` (resolves the nested `Contents/MacOS/<exe>` path). No entitlements/signing-pipeline changes needed beyond signing the extra bundle layer.
> - **Fixed (found via a real Phase 4.5 fork-release CI run, `arilieb/locksmith@feat-daemon-support`):** the "Embed kg-guardian / sentinel-daemon" CI step failed with `ValueError: Unable to find libsodium`. Root cause: `embed_daemons.py`'s `_daemon_app_info()` used `importlib.util.find_spec`/`exec_module` to fully **execute** `keriguard_user.core.daemon_launch` (added by the bundle-wrapping fix above) just to read the static `DAEMON_APP_INFO` dict off it. `daemon_launch.py` has `from keri import help` at module scope (for its logger) — importing `keri` pulls in `pysodium`, which `dlopen()`s libsodium as an import-time side effect, the same landmine `main.py`'s `load_custom_libsodium()` bootstrap exists to work around (commit `9f7d862`). But that bootstrap only runs inside the frozen app's `main.py`; `embed_daemons.py` runs in the plain CI build venv (`/tmp/venv`), which has no system/Homebrew libsodium, so the bare `import keri` chain crashed. Fixed by splitting `DAEMON_APP_INFO` out into a new leaf module, `keriguard_user.core.daemon_app_info` (no `keri`/`keystore` imports at all, by design — see that module's docstring), which `daemon_launch.py` now imports from, and pointing `embed_daemons.py`'s `_daemon_app_info()` at the leaf module instead of `daemon_launch`. No behavior change for the frozen app or for `daemon_launch`'s own callers.
> - **Fixed (found via a from-scratch manual runtime test against a fork-cut build, `locksmith` `be6f402`/`0e3a567`/`44158cc` tip):** both daemons crashed on **every single launch** — `launchctl bootstrap` reported success (it only registers the job), but `guardian.stderr.log`/`sentinel.stderr.log` showed an identical PyInstaller bootloader error each time: `Failed to load Python shared library '.../_MEIxxxx/Python': ... code signature ... not valid for use in process: mapping process and mapped file (non-platform) have different Team IDs`. Root cause: `kg-guardian`/`sentinel-daemon` are built as PyInstaller **onefile** executables (`guardian_a`/`sentinel_a` in `Locksmith.spec`, no `exclude_binaries=True`/`COLLECT`, unlike the main `KERIGuard.app` Analysis at the top of the same file) — onefile bootloaders self-extract their bundled `Python.framework` to a fresh temp dir on every launch and `dlopen()` it from there. `sign.sh` only ever signed the outer `kg-guardian`/`sentinel-daemon` executable and its wrapper `.app`; it never re-signed that embedded `Python.framework` payload, because the payload isn't a real file on disk until runtime extraction, long after signing already ran. So the extracted copy kept whatever signature it had from the build environment, not `DEVELOPER_ID_APP_CERT`'s Team ID — and Hardened Runtime's Library Validation (active via `--options runtime`, requires every non-platform dylib a signed process loads to share its Team ID) rejected the load every time, crash-looping both daemons under `KeepAlive`. This is exactly why the socket/heartbeat never appeared: the process never got past its own interpreter bootstrap. The main app never hits this because it's built onedir, so its own `Python.framework` is a real on-disk file `sign.sh`'s existing `Contents/Frameworks` codesign loop already re-signs correctly. **Fixed** by adding `entitlements-daemon.plist` (new file, just `com.apple.security.cs.disable-library-validation`) and passing `--entitlements entitlements-daemon.plist` when signing the two daemon executables (not the wrapping `.app`, and not the main `KERIGuard.app`) in `sign.sh` — the standard escape hatch for PyInstaller-onefile-under-hardened-runtime, rather than reworking both daemons to onedir packaging.
>
> **Still open / blocked:**
> 1. **Not build-verified** — no full `pyinstaller Locksmith.spec` run performed (per the packaging-build gate, this is correctly deferred to Phase 5; also this sandbox doesn't have the full `locksmith` venv — PySide6, `keri`, etc. — installed). Spec/script changes are syntax-checked only (`ast.parse`). **Superseded for the CI path specifically** by Phase 4.5 below, which does run real `pyinstaller`/sign/notarize passes on GitHub's macOS runners — the libsodium fix above was found and fixed through that loop, not a local run.
> 2. **Pin `keriguard`'s `sentinel` dependency** (`keriguard/pyproject.toml:30`: `sentinel @git+https://git@github.com/healthKERI/sentinel@main`) to a tag — **blocked**, `sentinel` has no tags yet (Phase 1 tagging is still pending, same blocker noted in Risk #6).
> 3. **`locksmith` edits are uncommitted local changes**, same pending-review state as the rest of Phase 2 — not yet committed/pushed.

- Add two new PyInstaller targets (`Analysis`/`EXE()` entries in `Locksmith.spec`) producing frozen `kg`/`sentinel` executables from their existing console-script entry points: `keriguard/pyproject.toml:70` (`kg = "keriguard.app.cli.kg:main"` — **not** the `kg-sentinel` entry at line 71) and `sentinel/pyproject.toml:52` (`sentinel = "sentinel.app.cli.sentinel:main"`). Embed at e.g. `Contents/Resources/kg-guardian` / `Contents/Resources/sentinel-daemon`.
- Extend `sign.sh`'s existing signing loop to cover these two new executables — no new signing/notarization pipeline needed.
- Extend the entitlements audit (Phase 2b) to the new daemons' file paths (`.../sentinel/`, `server.bran`, heartbeat files) **plus the decided paths** (`~/.keriguard/{pre}` OTP artifact for the witnessed guardian, `sentinel/config.yaml` for the SaaS sentinel). The keystore tree itself (`--base plugins/keriguard-user`) is at the default `headDirPath`, not App-Support, so it isn't part of this audit.
- Pin `keriguard`'s `sentinel` dependency (`keriguard/pyproject.toml:30`: `sentinel @ git+https://git@github.com/healthKERI/sentinel@main`) to a tag once `sentinel` starts tagging releases (Phase 1).

---

## Phase 4.5 — Fork-based release CI verification [current phase, in progress]

Not part of the original phase numbering (hence the `.5`) — inserted between Phase 4 landing and Phase 5's manual verification checklist once it became clear the daemonization build needed a real, repeatable macOS CI pass (PyInstaller, codesign, notarization) before committing to the Phase 1 fork+review-then-merge-then-tag sequence. Running that full pipeline by hand on a single dev machine is slow and non-reproducible; cutting an actual `release.ci.yml` release from each repo's fork branch gets the same GitHub Actions macOS runners, signing identity, and notarization path Phase 5 will ultimately depend on, without touching any `main` branch or cutting a real tag yet.

**What this phase is:** `release.ci.yml`'s `build-macos-keriguard` job, run against `arilieb`'s personal forks instead of `healthKERI`'s repos, on each of the three fork branches in lockstep:
- `arilieb/locksmith@feat-daemon-support`
- `arilieb/keriguard-plugin@feat-daemon-support`
- (`sentinel`/`keriguard` are consumed by branch too — see `daemonize-phase1`/`feat-daemon-support` references in `release.ci.yml`'s install steps, not by tag, since Phase 1 tagging is still pending per Risk #6)

The job's `is-healthkeri` gate (`release.ci.yml:190-195`) keeps these fork-cut releases from clobbering the DO Spaces objects real users' installs point at — this is exactly the mechanism `f746d8f` added so test releases on a fork can't touch `current/`.

**Intent:** demonstrate a successful, fully-automated end-to-end build — PyInstaller freeze of `Locksmith.app` + `kg-guardian` + `sentinel-daemon`, daemon embedding (`embed_daemons.py`), codesigning, and (eventually) notarization — cut entirely from unmerged fork branches, *before* any of Phase 1's upstream patches are merged/tagged in `sentinel`/`keriguard`, and before the local `locksmith`/`keriguard-plugin` edits from Phases 2/4 are committed/pushed. A green run here is the evidence needed to greenlight the fork+review pass and tagging that Phase 1/Risk #6 are still blocked on — it substitutes a real CI signal for the "trust the local diff" position the Phase 2/4 status notes have been in.

**This phase includes a full manual runtime-verification pass, not just CI going green and not just "does the app launch."** Once `build-macos-keriguard` produces a signed `.app`/DMG from the fork-cut build, that artifact gets installed and actually exercised by hand end-to-end — vault open, Setup, provisioning, and the daemon runtime behavior itself: `launchctl bootstrap` of both agents, confirming they survive Locksmith quitting and a reboot, killing/`KeepAlive`-restart, the guardian heartbeat file advancing, witnessed AID provisioning, `sentinel watcher add` against the relocated socket, and the WireGuard-config end-to-end path via `KERIGuardHelper`. In other words, this phase runs (against the fork-cut build) substantially the same checks the Phase 5 checklist below enumerates — a green CI job only proves the packaging pipeline didn't error, not that the daemons actually run correctly. **This full manual pass has to be repeated once the same build is cut for real against `healthKERI/main`** (post Phase 1 merge+tag) — a passing fork-branch verification is evidence the daemonization approach and pipeline both work, but it doesn't carry over as proof for the `main`-branch artifact, since the fork pin (`@feat-daemon-support`) will have been swapped back to tagged `main` refs by then and needs its own from-scratch check.

**Status:**
- First real run surfaced the `embed_daemons.py`/libsodium bug documented in the Phase 4 status above (`ValueError: Unable to find libsodium` in the "Embed kg-guardian / sentinel-daemon" step) — a bug invisible to local `ast.parse`-only syntax checks and only reachable by actually executing the packaging scripts on a real runner. Root-caused to `_daemon_app_info()` executing `daemon_launch.py`'s full module body (including its `keri` import) in the bare CI venv; fixed by extracting `DAEMON_APP_INFO` into the `keri`-free `keriguard_user.core.daemon_app_info` leaf module.
- CI went green after the libsodium fix and the resulting DMG was installed and manually walked through the "Testing from scratch" flow. That pass surfaced a second, real runtime bug — `AF_UNIX path too long` on the relocated sentinel socket (see the Phase 1a status note above for the fix) — plus an as-yet-unexplained observation that the daemons still showed up under the developer's own name in Login Items & Extensions rather than their `KERIGuardGuardian.app`/`KERIGuardSentinel.app` branding; that one needs to be re-checked against a build actually cut from the current fork branch tip (the DMG tested may have predated the wrapper-bundle fix landing).
- Re-cut the release with the socket-filename fix and redid the manual verification pass. CI/embedding/signing all passed and the app installed cleanly, but both daemons crash-looped on **every** launch — `guardian.stderr.log`/`sentinel.stderr.log` showed a PyInstaller onefile bootloader error (`Python.framework ... different Team IDs`, a Hardened Runtime Library Validation rejection — see the Phase 4 status note above for the full root cause and fix) on every single restart, which is why the sentinel socket never appeared and the guardian heartbeat was stale — the daemons never got past loading their own embedded interpreter. Fixed by adding `entitlements-daemon.plist` (`com.apple.security.cs.disable-library-validation`) to the two daemons' `codesign` step in `sign.sh`. The Login Items branding question from the prior pass is still unresolved either way (a crash-looping process is not a fair test of it) and needs to be re-checked once the daemons actually stay up.
- Re-cut the release with the `entitlements-daemon.plist` fix (tag `v0.0.1-alpha`, commit `7086295`) and redid the manual verification pass — **same crash, identical `different Team IDs` error, on both daemons.** Root cause: `sign.sh`'s per-daemon loop signed the executable with `--entitlements entitlements-daemon.plist` (inner) and then force-re-signed the wrapping `.app` bundle with **no `--entitlements` flag** (outer). Verified empirically (throwaway ad-hoc-signed bundle, `codesign -d --entitlements :-` before/after) that codesign's bundle-level sign rewrites the same underlying Mach-O binary's signature — the second, entitlements-less sign silently discarded the `disable-library-validation` entitlement the first sign had just applied, so the shipped daemon binaries never actually carried it despite the fix being genuinely present in the tagged commit. The identical pattern (`entitlements.plist` on the raw `Contents/MacOS/$APP_NAME`, then a bare re-sign of the whole `KERIGuard.app`) also existed for the **main app**, meaning the Phase 2b sandbox/file entitlements were likely being silently dropped from every shipped build too, not just the daemons. Fixed by re-passing `--entitlements` on both bundle-level `codesign` calls in `sign.sh` (daemon bundles → `entitlements-daemon.plist`, main app bundle → `entitlements.plist`). A stray syntax error (missing `fi`) introduced while applying this fix was caught via `bash -n` and corrected in the same pass.
- Re-cut the release with the bundle-level entitlements fix and redid the manual verification pass. Signing/embedding were correct this time, but the very first step — vault creation, before either daemon is even touched — now failed: `CreateVaultDialog.create_vault()` (`locksmith/src/locksmith/ui/vaults/create.py`) raised `lmdb.Error: .../Library/Containers/com.healthkeri.locksmith/Data/.keri/ks/admin: Operation not permitted` out of `habbing.Habery.__init__` → `keeping.Keeper.__init__` → `hio.base.filing.Filer.reopen`. Root cause: `com.apple.security.app-sandbox` (`entitlements.plist`) relocates the sandboxed process's `$HOME` under `~/Library/Containers/com.healthkeri.locksmith/Data/`, and while file *read/write* access to that tree can be (and was, across `f06f775`/`1fd42a0`) granted via `temporary-exception.files.absolute-path.read-write`, LMDB's `flock()`-based file locking on `.../ks/admin/data.mdb` is a distinct sandbox-mediated operation that the sandbox's private, undocumented SBPL vocabulary has no dedicated per-path exception for — `1fd42a0`'s `(allow file-lock (home-subpath ...))` raw-SBPL attempt (a documented-workaround pattern, not an Apple-sanctioned entitlement) still didn't clear it. **Decision: since this build is distributed outside the App Store, drop the sandbox entirely instead of continuing to chase SBPL exceptions for it.** `entitlements.plist` was cut down to just the three Hardened-Runtime `com.apple.security.cs.*` entitlements (`allow-jit`, `allow-unsigned-executable-memory`, `allow-dyld-environment-variables` — still needed, since `sign.sh` still signs with `--options runtime` for notarization regardless of sandboxing); `com.apple.security.app-sandbox`, `network.client`/`network.server`, the three `device.*` entitlements, `files.user-selected.read-write`, and both `temporary-exception.*` arrays (including the `1fd42a0` SBPL workaround) were all removed as sandbox-only entitlements that do nothing once `app-sandbox` is unset. `create.py`'s own debugging attempt at this problem — passing a hand-built `keeping.Keeper`/commented-out `configing.Configer` into `habbing.Habery(ks=...)` to try to route around the lock failure at the LMDB layer — was reverted back to the plain `habbing.Habery(...)` call once the sandbox itself was removed; the fix belongs at the entitlements layer, not the KERI layer. `entitlements-daemon.plist` (daemon-only `disable-library-validation`) is unaffected — it was never a sandbox entitlement. No changes needed to `Locksmith.spec`/`sign.sh`/CI — nothing else in the pipeline referenced `app-sandbox` or `Containers/`.
- Re-cut the release with the sandbox removed and redid the manual verification pass — **past vault creation this time**, but both daemons now fail immediately on launch: `guardian.stderr.log`/`sentinel.stderr.log` show, on every single invocation, `kg-guardian: error: unrecognized arguments: guardian start --base plugins/keriguard-user --name ... [full ProgramArguments]` / `sentinel-daemon: error: unrecognized arguments: start --config ... [full ProgramArguments]`, each preceded by a bare `usage: kg-guardian [-h]` / `usage: sentinel-daemon [-h]` with **no subcommands listed at all**. Downstream of this, `daemon_watch.py`'s 5-retry sentinel-socket wait times out (`Sentinel socket not found at .../sentinel_cQKhHSvi.sock`) and the guardian heartbeat never advances (`WARNING guardian daemon heartbeat is stale`) — both are symptoms of the daemons never getting past argument parsing, not separate bugs. Root cause: `keriguard.app.cli.kg:main` and `sentinel.app.cli.sentinel:main` (the two console-script entry points `scripts/entrypoints/{kg_guardian_entry,sentinel_daemon_entry}.py` wrap) both build their argparse tree via `multicommand.create_parser(commands)`, which walks the `commands` sub-package's `__path__` with `pkgutil.iter_modules()` **at runtime** to discover subcommand modules (`commands/guardian/start.py`, `commands/start.py`, etc.) — nothing in either CLI's source ever imports those submodules directly by name. PyInstaller's `Analysis` only bundles what its static import graph can see, so with `hiddenimports=[]` on both `guardian_a`/`sentinel_a` blocks in `Locksmith.spec`, every dynamically-discovered subcommand module was silently dropped from the frozen archive — `multicommand` doesn't error on finding zero subcommands, it just builds a parser with none, hence "unrecognized arguments" for literally everything after the bare `[-h]` flag. Same class of bug as the `daemon_app_info`/libsodium fix above (dynamic discovery invisible to PyInstaller's static analysis), one layer further down the stack (CLI dispatch instead of module import). **Fixed** by adding `hiddenimports=collect_submodules("keriguard.app.cli.commands")` / `collect_submodules("sentinel.app.cli.commands")` (from `PyInstaller.utils.hooks`, already imported for `collect_data_files`/`copy_metadata`) to the respective `Analysis` blocks in `Locksmith.spec`, force-collecting every submodule under each `commands` package regardless of whether the frozen entry point statically references it.
- Immediate next step: re-cut the fork release with the sandbox-removal and `collect_submodules` fixes included, then redo the manual verification pass end-to-end (vault creation, both daemons actually starting and registering subcommands correctly, Login Items branding, and spot-checking `codesign -d --entitlements :- KERIGuard.app` on the shipped main app — the entitlements now expected there are just the three `com.apple.security.cs.*` keys, no `app-sandbox`).

**Still open:**
1. Re-run `build-macos-keriguard` on `arilieb/locksmith@feat-daemon-support` with the `daemon_app_info` fix, the `sentinel` socket-filename fix, the daemon `disable-library-validation` entitlement fix, **the no-sandbox `entitlements.plist`**, and **the `collect_submodules` CLI-discovery fix**, and confirm the "Embed kg-guardian / sentinel-daemon" step (and everything downstream of it — signing, DMG creation) passes.
2. Once CI is green, install and manually run the full Phase 5 verification checklist below against the fork-cut build: vault open → Setup → witnessed provisioning, `launchctl bootstrap` of both daemons, survives-quit/reboot, `KeepAlive` restart-on-kill, heartbeat advancing, `sentinel watcher add` against the relocated socket, and the end-to-end WireGuard-config path via `KERIGuardHelper`. This is part of Phase 4.5, not deferred to a later phase.
3. This phase's job is then done — it does **not** itself merge or tag anything. Proceeding to fork+review, merge-to-`main`, and tagging in `sentinel`/`keriguard`/`keriguard-plugin` (Risk #6) remains a separate, manual step gated on both #1 and #2 above.
4. **Re-do the full manual runtime verification (#2) once a build is cut against `healthKERI/main`** post-merge/tag — the fork-branch build passing doesn't substitute for checking the real `main`-pinned artifact. That repeat pass is what the "Verification checklist (Phase 5)" section below actually refers to.

---

## Open Risks
| # | Risk | Resolved in |
|---|------|-------------|
| 1 | ~~Three independent `/tmp/sentinel_{hab.pre}.sock` hardcodings must move in lockstep~~ **IMPLEMENTED** as a threaded `socket_dir` + `sentinel start --socket-dir` (`sentinel` `3b67423`); residual risk is that the flag defaults to `/tmp`, so every macOS caller (plist *and* in-process `LocalWatcherConnector`) must pass it | Phase 1a (code complete; verify via `setup_hk` in Phase 5) |
| 2 | ~~`keriguard-plugin` has no CI/tags~~ **IMPLEMENTED**: CI added, versions bumped to `0.1.0`, `release.ci.yml` pinned to `@v0.1.0` (tag not yet cut/pushed). `keriguard`'s `sentinel` dependency is still unpinned | Phase 2a (code complete; tag pending) / Phase 4 |
| 3 | `KERIGUARD_HELPER_VERSION` defaults to `"current"` if unset | Phase 2c |
| 4 | ~~Does the guardian AID need a witness?~~ **DECIDED: yes.** Provision via the OTP-based flow (`reserve_witness_for_server` → OOBI-load → `authenticate_witness` → `rotate_witness`); OTP-secret artifact actually lands at the hardcoded `~/.keriguard/{server_hab.pre}` (not App-Support — corrected from an earlier draft), write-only, not read at daemon start | Decisions (decided) → provisioning + Phase 3c `ProgramArguments` |
| 5 | ~~Neither daemon CLI has a custom-`headDirPath` flag~~ **REVERSED — no flag needed.** The `--data-dir` patch (`sentinel` `3b67423`, `keriguard` `56ee181`) was reverted from both upstream repos; both now build against unmodified `main` for this piece. `provisioning.py`/the launchd plists instead use the pre-existing `--base plugins/keriguard-user` flag at the default `headDirPath`, same location the human vault's own Haberies already use. Haberies are still distinguished from each other by `--name`/`--alias` as before | Decisions (reversed) |
| 6 | Phase 1 branches are unmerged and **untagged**, so Phase 4 cannot pin yet; a review pass could still change the flag surface the Phase 3 plists depend on | fork+review → merge → tag (blocks Phase 4); **Phase 4.5 (current)** is proving the Phase 4 pipeline green on real CI, cut from forks, before that fork+review pass is greenlit |
| 7 | ~~Phase 3 daemon-launch code cannot actually launch anything~~ **Phase 4 code complete**: `Locksmith.spec` now builds `kg-guardian`/`sentinel-daemon` (guarded on `keriguard_user` being part of the flavor) and `scripts/embed_daemons.py` embeds them + the launchd plist templates into `Contents/Resources/`; `sign.sh` signs both. Still **unverified** — no real PyInstaller build run yet (see Phase 4 status) | Phase 4 (code complete, unverified) → Phase 5 |

---

## Verification checklist (Phase 5)

Run after Phase 4 is substantially in place — this is when the packaging build is re-verified (per the prerequisite gate). **This is the same checklist Phase 4.5 runs against the fork-cut build** (see Phase 4.5's "Still open" #2) — Phase 5 is that pass repeated from scratch against the real `healthKERI/main`-cut build once Phase 1 is merged/tagged and Phase 4.5 is green, not a distinct set of checks.

### Upstream Phase 1 behavior at runtime (validates Phase 1)

Unit tests pass in both repos already; these check the parts only a real daemon run can prove:
- [ ] Branches merged to `main` and **tagged** in both `sentinel` and `keriguard` (unblocks Phase 4 pinning).
- [ ] With `--socket-dir` passed, the socket appears under `~/Library/Application Support/KERIGuard/sentinel/` and **nothing** is created in `/tmp` — verified through the `setup_hk` (SaaS) path, and `watcher add` / the plugin's `LocalWatcherConnector` connects to it.
- [ ] `--passcode-file ~/.../server.bran` opens the keystore with no passcode on the command line (`ps` shows no secret), and `--passcode` is absent from the plist.
- [ ] With `--base plugins/keriguard-user`, both daemons reopen the **provisioned** Habery (correct `--name`/`--alias`) and do **not** silently fall back to a default/empty keystore.
- [ ] `guardian.heartbeat` mtime advances once per poll cycle on an idle daemon, and goes stale when the daemon is killed (`guardian_check.py` threshold is poll-interval-derived).

### Packaging cleanup (validates Phase 2)
- [ ] `sign.sh` signs against the checked-in `entitlements.plist` (no drift possible).
- [ ] Real sandboxed smoke test with the new file entitlements in place: vault open → Setup end-to-end → `KERIGuardHelper.app` launch.
- [ ] CI **fails** (does not silently default) when `KERIGUARD_HELPER_VERSION` is unset.
- [ ] A tagged release installs cleanly via the pinned CI reference.

### Daemon lifecycle (validates Phase 3/Phase 4)
- [ ] Launch both daemons via `launchctl bootstrap`; `launchctl print gui/<uid>/<label>` shows them running **after Locksmith quits** and **after a reboot**.
- [ ] Kill each process; confirm `KeepAlive` restarts it.
- [ ] **Clean wipe before provisioning:** confirm the App-Support tree (and `~/.keriguard/`, and `<default_head>/keri/ks/plugins/keriguard-user/`) is fully cleared before the from-scratch witnessed provisioning — no leftover `witness=False` state (`server.bran`, `sentinel/config.yaml`, `~/.keriguard/{old_pre}`, the old keystore tree) that a daemon could silently reopen via `--base plugins/keriguard-user` and pick up the wrong AID shape.
- [ ] Confirm both daemons reopen their respective provisioned Haberies via `--base plugins/keriguard-user` — no fallback to a default location, no "Habery not found" at start.
- [ ] Confirm each daemon opens its **own `name`-distinguished Habery** (distinct LMDB environment, both sharing `base=plugins/keriguard-user`) under the shared default `headDirPath`, with explicit `--name`/`--alias` matching what provisioning wrote — and that guardian and sentinel do **not** share one LMDB environment (no concurrent-writer contention).
- [ ] Confirm `locksmith.ui.vaults.drawer`'s vault list shows the `plugins/keriguard-user` base folder as a navigable entry, distinct from the human vault(s) at the root.
- [ ] Confirm `sentinel_launch.py` generates `~/Library/Application Support/KERIGuard/sentinel/config.yaml` **before** the plist, and the daemon starts cleanly in SaaS mode from `--config` (no `--name`/`--alias`/`--local`).
- [ ] Confirm the guardian AID is provisioned **with a witness** (OTP flow completed; `server.otp` present, mode `0600`) and the daemon starts with the witnessed `ProgramArguments`.
- [ ] Exercise `sentinel watcher add` end-to-end against the relocated socket path (via the `setup_hk`/SaaS path).
- [ ] Confirm the guardian heartbeat file updates each poll cycle.

### End-to-end
- [ ] Issue a real interface/connection credential to the new (witnessed) guardian AID (`kg interface create` / `kg peers connect`); confirm the guardian daemon picks it up and writes/reloads the WireGuard config via the existing `KERIGuardHelper` IPC path — **entirely with Locksmith closed and the vault locked**.