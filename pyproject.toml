# This repo contains two independently installable Locksmith plugins.
#
#   Admin plugin:   pip install -e admin/
#   Client plugin:  pip install -e client/
#
# See admin/pyproject.toml and client/pyproject.toml for each package.

[tool.pytest.ini_options]
pythonpath = ["."]